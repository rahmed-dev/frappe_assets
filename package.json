{
	"name": "frappe-assets",
	"version": "0.2.0",
	"description": "Desk dashboard toolkit for Frappe apps",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/rahmed-dev/frappe_assets.git"
	},
	"type": "module",
	"//sideEffects": "The toolkit is pure — every module earns its place by being imported for an export, so a consuming bundler may drop whatever a dashboard does not use. `demo/` is the exception and must be listed: `gallery.js` pulls in `frappe-stub.js` as a bare import for its side effects alone, and under a blanket `false` esbuild removes that import and the gallery dies on the first `__()`.",
	"sideEffects": [
		"demo/**"
	],
	"//exports": "Two entries and no wildcard, on purpose. `CONTRACT.md` promises the index exports and the stylesheet; a deep import of any other module is not supported, and listing `./dashboard/*` would turn every internal file into an accidental contract that nothing then dares rename. The SCSS is reachable by path because an app's stylesheet imports it as `~frappe-assets/dashboard/dash.scss` through sass includePaths, which does not consult this field — the entry is here so a bundler that does consult it agrees.",
	"exports": {
		"./dashboard": "./dashboard/index.js",
		"./dashboard/dash.scss": "./dashboard/dash.scss",
		"./package.json": "./package.json"
	},
	"files": [
		"dashboard"
	],
	"scripts": {
		"demo": "node demo/build.mjs"
	},
	"peerDependencies": {
		"echarts": "^5.6.0"
	},
	"//devDependencies": "Gallery build only. Yarn does not install a git dependency's devDependencies, so none of this reaches a consuming app. The script is named `demo` and not `prepare` for the same reason: yarn runs `prepare` for git deps, and every app installing this would start building the gallery.",
	"devDependencies": {
		"echarts": "^5.6.0",
		"esbuild": "^0.25.0",
		"sass": "^1.83.0"
	}
}
